<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LCOGT Planet Database</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <main>
      <%- include('partials/nav', {active: "Home"}); %>
      <section>
        <h2>Current Entries</h2>
        <div>
          <% for (var i = 0; i < entries.length; i++) { %>
          <div key="<%= `${entries[i].id}` %>">
            <h3><a href="<%= `planets/detail/${entries[i].name}` %>"><%= entries[i].name %></a></h3>
          </div>
          <% } %>
        </div>
      </section>
  </main>
  </body>
</html>

<!--
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LCOGT Planet Database</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <main>
      <%- include('partials/nav', {active: "Home"}); %>
      <section>
        <h2>Current Entries</h2>
        <article>
          <% for (var i = 0; i < entries.length; i++) { %>
          <div key="<%= `${entries[i].id}` %>">
            <h3><a href="<%= `planets/detail/${entries[i].name}` %>"><%= entries[i].name %></a></h3>
            <h4><%= entries[i].size %> Earth Masses</h4>
            <h4><%= entries[i].distance %> AU</h4>
            <p><%= entries[i].description %></p>
          </div>
          <% } %>
        </article>
      </section>
  </main>
  </body>
</html>
-->
